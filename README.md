# NodeJS + Vite Authentication Setup

## Environment Setup

### Server

In the server folder, create a file config.env and in that create the following variables:

```
PORT=
MONGO_URL=
ACCESS_TOKEN=
REFRESH_TOKEN=
```

---
